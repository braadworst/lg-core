{
  "name": "lr-main",
  "version": "2.0.7",
  "description": "The core for everything lagoon-road",
  "main": "index.js",
  "scripts": {
    "test": "nyc tape test/*.js | tap-spec",
    "report": "nyc report --reporter=html",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "all": "npm run test && npm run report && npm run coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lagoon-road/lr-main.git"
  },
  "author": "Roy Niels",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lagoon-road/lr-main/issues"
  },
  "keywords": [
    "core",
    "lagoon-road"
  ],
  "homepage": "https://github.com/lagoon-road/lr-main#readme",
  "dependencies": {
    "check-types": "^7.1.5"
  },
  "devDependencies": {
    "coveralls": "^2.13.0",
    "lr-server-router": "^1.0.0",
    "nyc": "^10.2.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3"
  }
}
